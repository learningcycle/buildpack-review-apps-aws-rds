#!/usr/bin/env bash

set -e

BUILD_DIR=$1
BIN_DIR=$(cd $(dirname $0); pwd)

source "$BIN_DIR/support/functions"

cat $BUILD_DIR/tmp/heroku-buildpack-release-step.yml
cat $BUILD_DIR/tmp/buildpack-review-apps-aws-rds-release-step.yml
